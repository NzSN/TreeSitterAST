# Work Report: Implement Inheritance for CHOICE Node Alternatives

**Date**: 2025-03-09 18:45
**Task**: Complete the work in works.md: "classes from alternatives of a top-level CHOICE node should inherit from class from the top-level CHOICE node."

## Changes Made

### File Modified
1. **`src/ProgBuilder/ECMA/ProgBuilderForECMA.hs`**:
   - Updated `build` function signature to accept parent map: `build :: String -> TSGN.Node -> Map.Map String String -> T.Text`
   - Added `parentMap` parameter to track parent-child relationships for CHOICE nodes
   - Implemented `computeParentMap` function that identifies alternative rules (names with underscore) whose parent rule is an empty CHOICE node (top‑level CHOICE emptied by `transformGrammarWithChoiceSplitting`)
   - Modified class generation logic: if a rule is an alternative of a top‑level CHOICE node, its generated class inherits from the parent CHOICE class instead of directly from `SyntaticInterior`

## Implementation Details

### Parent Map Detection
- `computeParentMap` scans all rule names in the transformed grammar
- For each rule name containing an underscore, splits into prefix (parent) and suffix
- If the parent rule exists and is an empty `Choice []` (i.e., a top‑level CHOICE that has been emptied), adds mapping `alternative -> parent`
- This mapping is used in `build` to determine the correct base class

### Inheritance Logic
- In `build`, after determining the default base class (`SyntaticLeaf` or `SyntaticInterior`), checks whether the rule name appears in the parent map
- If yes, uses the parent CHOICE class (computed via `node_type_ident`) as the base class
- All other generation (fields, constructors, evaluate methods) remains unchanged

## Verification

### Generated TypeScript Output
- Verified that alternative classes now correctly extend their parent CHOICE class:
  - `Statement_with_statement_T extends Statement_T`
  - `Statement_while_statement_T extends Statement_T`
  - `String_0_T extends String_T`
  - `String_1_T extends String_T`
  - `Declaration_*_T extends Declaration_T`
  - `Expression_*_T extends Expression_T`
- Parent CHOICE classes remain empty with error‑throwing `evaluate()` methods (as before)

### Test Suite
- Ran existing test suite: **all 20 tests pass**
- No regressions in inference, parsing, or code generation

## Notes
- Inheritance only applies to alternatives of **top‑level CHOICE nodes that have been emptied** (those whose members are removed by `transformGrammarWithChoiceSplitting`)
- Some CHOICE nodes (e.g., `primary_expression`) are not emptied (they remain with members) and therefore their alternatives do not inherit from the parent – this is consistent with the requirement
- The implementation respects the existing codebase patterns and uses the established `node_type_ident` helper for TypeScript class naming

## Generated Files
- Output directory: `agent_generated/`
- Generated TypeScript: `javascript_template.ts`
- Grammar representation: `javascript_nodes.txt`

## Conclusion
The requirement is satisfied: classes from alternatives of a top‑level CHOICE node now inherit from the class of the top‑level CHOICE node, enabling proper polymorphism in the generated TypeScript AST.